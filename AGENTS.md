# opencv-vision-system · AI 速查卡

> 一句话：基于 OpenCV 的综合视觉识别系统 —— 11 色自适应检测 + 几何形状分类 + 实心/空心圆判定，双线程流水线。
> **详细说明看 `README.md`**。本卡只放"不看就会犯错"的东西。

---

## 铁律

**1. 核心是单个类 `VisionSystem`**，位于 `src/综合视觉识别系统.py`。
改算法去那里改，**不要在 `examples/` 里改逻辑** —— 那是调用示例。

**2. 圆形判定用严格数学圆形度 `C = 4πA/P²`**，配合轮廓拓扑区分**实心圆**与**空心圆环**。
⚠️ **不要用宽高比或面积阈值替代** —— 那正是无法区分这两者的原因，也是这个项目存在的理由。

**3. 双线程架构**：采集线程与预处理/二值化线程分离，用 `threading.Lock` 互斥帧。
改并发逻辑时注意**锁的粒度与获取顺序**，这个结构的设计目标就是避免死锁。

**4. 无相机时自动回退到内置测试图** —— 这是**特性不是 bug**，离线验证与 CI 依赖它。

**5. 颜色是 11 种**（红橙黄绿青蓝紫粉白灰黑），HSV + BGR 双空间自适应判定。
**新增颜色要同时改两个空间**，只改一个会在部分光照下失效。

---

## 目录

```
src/综合视觉识别系统.py   VisionSystem 核心类（颜色/形状/圆形分类器 + 双线程流水线）
examples/demo.py          多源实时 Demo（相机 0/1 → 视频 → 图片 → 自动回退）
examples/make_sample.py   纯代码生成标准测试样本图
assets/                  静态资源
requirements.txt         Python 3.8+ / OpenCV 4.7+
test_circle.png          圆形判定测试图
```

---

## 常用

```bash
pip install -r requirements.txt
python examples/demo.py           # 实时运行（无相机时自动用测试图）
python examples/make_sample.py    # 生成标准测试样本
```

---

## 注意

- **这是公开仓库** —— 改动会影响外部使用者。破坏性变更（改类名、改方法签名）必须在 `README.md` 里写清楚。
- 适用场景：智能车/机器人竞赛视觉任务、靶标检测、嵌入式 CV 教学。
